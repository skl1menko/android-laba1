# Лабораторна робота: Navigation UI з Kotlin

## Опис проєкту
Цей Android-додаток демонструє роботу з Navigation Component, Compose UI та різними підходами до управління станом:
- **remember** - простий стан, який НЕ зберігається при зміні конфігурації
- **rememberSaveable** - стан, який ЗБЕРІГАЄТЬСЯ при зміні конфігурації (орієнтація, мова тощо)
- **ViewModel** - архітектурний компонент для збереження стану та бізнес-логіки

## Структура навігації
Проєкт містить три екрани з унікальним UI та функціональністю:

### 1. Головна (Home) 🏠
- Використовує **remember** для демонстрації втрати стану
- Лічильник натискань
- Динамічна мітка, яка оновлюється при натисканні
- **Експеримент**: При зміні орієнтації екрану стан втрачається

### 2. Профіль (Profile) 👤
- Використовує **rememberSaveable** для збереження стану
- Лічильник відвідувань
- Динамічне оновлення тексту
- **Експеримент**: При зміні орієнтації екрану стан зберігається

### 3. Налаштування (Settings) ⚙️
- Використовує **ViewModel** для управління станом
- Демонструє найкращу практику для складних екранів
- Лічильник з збереженням стану через ViewModel
- **Експеримент**: Стан зберігається навіть при зміні конфігурації

## Файли проєкту

### MainActivity.kt
Головна активність, яка налаштовує навігацію з Scaffold та BottomNavigationBar.

### Navigation.kt
Містить:
- `enum class AppDestinations` - елементи навігації (HOME, PROFILE, SETTINGS)
- `AppNavigationGraph` - граф навігації з маршрутами
- `HomeScreen` - екран з remember
- `ProfileScreen` - екран з rememberSaveable
- `SettingsScreen` - екран з ViewModel
- `AppBottomNavigationBar` - нижня панель навігації

## Особливості реалізації

### 1. Enum клас AppDestinations
```kotlin
enum class AppDestinations(val title: String, val icon: ImageVector) {
    HOME("Головна", Icons.Filled.Home),
    PROFILE("Профіль", Icons.Filled.Person),
    SETTINGS("Налаштування", Icons.Filled.Settings)
}
```

### 2. Різні підходи до управління станом
- **remember**: Простий стан для UI елементів, які не потребують збереження
- **rememberSaveable**: Для збереження стану при повороті екрану
- **ViewModel**: Для складної логіки та збереження стану між зміною конфігурації

### 3. UI компоненти
Кожен екран містить:
- **Text** - мітка з динамічним текстом
- **Button** - кнопка для зміни стану
- **Card** - контейнер для групування елементів
- Унікальне вирівнювання та розміри

## Запуск проєкту

### Варіант 1: Через Android Studio
1. Відкрийте проєкт в Android Studio
2. Дозвольте Gradle синхронізуватися
3. Створіть емулятор через AVD Manager або підключіть реальний пристрій
4. Натисніть "Run" (зелена кнопка)

### Варіант 2: Через командний рядок
```bash
# Збірка проєкту
./gradlew build

# Встановлення на пристрій/емулятор
./gradlew installDebug
```

### Варіант 3: Якщо виникають проблеми з Java версією
Перевірте версію Java:
```bash
java -version
```

Встановіть Java 17 (рекомендовано для Android Studio):
```bash
# macOS (через Homebrew)
brew install openjdk@17

# Додайте до ~/.zshrc:
export JAVA_HOME=$(/usr/libexec/java_home -v 17)
```

## Експерименти для дослідження

### Експеримент 1: Зміна орієнтації екрану
1. Запустіть додаток
2. Перейдіть на екран "Головна"
3. Натисніть кнопку кілька разів
4. Поверніть екран (Ctrl+F11 на емуляторі)
5. **Результат**: Лічильник скинувся (remember не зберігає стан)

### Експеримент 2: rememberSaveable vs remember
1. Перейдіть на екран "Профіль"
2. Натисніть кнопку "Оновити профіль"
3. Поверніть екран
4. **Результат**: Текст залишився (rememberSaveable зберігає стан)

### Експеримент 3: ViewModel
1. Перейдіть на "Налаштування"
2. Натисніть кнопку кілька разів
3. Поверніть екран
4. **Результат**: Лічильник зберігся через ViewModel

### Експеримент 4: Навігація
1. Змініть стан на різних екранах
2. Переключайтеся між екранами через нижню панель
3. **Результат**: Стан зберігається завдяки `saveState = true` та `restoreState = true`

## Технології та бібліотеки
- Kotlin 2.0.21
- Jetpack Compose
- Navigation Compose 2.7.5
- Material 3
- ViewModel Compose 2.6.1
- Android Gradle Plugin 8.13.0

## Висновки
Цей проєкт демонструє:
1. ✅ Створення Navigation UI з мінімум 3 екранами
2. ✅ Власні елементи навігації в enum class
3. ✅ UI компоненти (Text, Button) на кожному екрані
4. ✅ Різне вирівнювання та розміри для кожного екрану
5. ✅ Обробники подій для кнопок
6. ✅ Дослідження remember vs rememberSaveable
7. ✅ Використання ViewModel як альтернативи

## Автор
Створено для демонстрації роботи з Android Navigation Component та управління станом у Jetpack Compose.

